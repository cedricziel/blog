{% form_theme postForm 'bootstrap.html.twig' %}

{% extends 'layout.html.twig' %}

{% block title %}New - PostAdmin{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <h1>New Post</h1>

                {{ form_start(postForm, {'attr': {'class': 'form-horizontal'}}) }}
                {{ form_widget(postForm) }}
                {{ form_end(postForm) }}
            </div>
        </div>
    </div>
{% endblock %}
